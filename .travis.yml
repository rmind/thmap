sudo: required
dist: xenial

language: c

compiler:
  - gcc
  - clang

script:
  # Run the unit tests and a stress test.
  - cd src && make tests && make stress
  # Run the unit tests with ASAN enabled.
  - cd src && DEBUG=1 make tests
